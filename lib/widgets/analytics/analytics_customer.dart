import 'package:flutter/material.dart';

class AnalyticsCustomer extends StatelessWidget {
  const AnalyticsCustomer({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}