import 'package:flutter/material.dart';

class AnalyticsTask extends StatelessWidget {
  const AnalyticsTask({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}